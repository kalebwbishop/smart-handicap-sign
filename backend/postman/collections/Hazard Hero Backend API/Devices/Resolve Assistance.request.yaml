$kind: http-request
description: |-
  Resolve an assistance request — moves back to available.

  Auth: Bearer `{{access_token}}`
url: "{{base_url}}/api/v1/devices/{{serial_number}}/resolve"
method: POST
order: 5000
