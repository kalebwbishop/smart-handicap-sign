$kind: http-request
description: |-
  Get events for a device. User must be a member of the device's organization.

  Query params:
  - `skip` (optional)
  - `limit` (optional)

  Auth: Bearer `{{access_token}}`
url: "{{base_url}}/api/v1/devices/{{serial_number}}/events"
method: GET
order: 10000
