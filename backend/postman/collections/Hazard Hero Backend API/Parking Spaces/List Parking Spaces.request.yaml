$kind: http-request
description: |-
  List all parking spaces for a site.

  Query params:
  - `skip` (optional)
  - `limit` (optional)

  Auth: Bearer `{{access_token}}`
url: "{{base_url}}/api/v1/sites/{{site_id}}/parking-spaces"
method: GET
order: 1000
